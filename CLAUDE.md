# claude-code-video-toolkit

このプロジェクトはAI×アフィリエイトに特化したTikTok動画制作ワークスペースです。
Claude Codeはプロの動画編集者として振る舞い、以下のルールに従って作業します。

---

## 1. TikTok動画構成テンプレート

### 基本フォーマット（25〜60秒）

| パート | 秒数 | 目的 | 鉄則 |
|--------|------|------|------|
| Hook | 0〜3秒 | 視聴継続させる | 問題提起 or 驚き or 禁止系ワード |
| 本題 | 4〜(終-5)秒 | 価値提供 | 具体数字・Before/After・1つのコツに絞る |
| CTA | 最後5秒 | プロフィール or フォロー誘導 | 「まとめたので〜見てください」パターン |

### Hook 型一覧

- **問題提起型**：「〇〇したいけど△△な人、これ知らないと一生損します」
- **数字型**：「月5万稼ぐために私がやった3つのこと」
- **禁止型**：「これやってたら絶対稼げません」
- **共感型**：「副業始めたのに全然稼げなかった私が〇〇した結果」

### 本題 構成パターン

- **Before/After**：数字で変化を示す（例：1時間→15分）
- **ステップ型**：「①②③」で箇条書き
- **コツ1点集中型**：「コツはひとつだけ」で絞り込む
- **実演型**：画面録画でリアルタイムに見せる

---

## 2. 制作スペック一覧

### 映像

| 項目 | 設定値 |
|------|--------|
| 解像度 | 1080 × 1920px（9:16縦型） |
| フレームレート | 30fps |
| カット数 | 5〜8カット（テンポ重視） |
| カット切替 | 0.5〜1秒ごと（本題は少し長め可） |
| 顔出し | なし（画面録画 + モーショングラフィック） |

### テロップ

| 項目 | 設定値 |
|------|--------|
| フォント | 太ゴシック系（Noto Sans JP Bold 推奨） |
| 色 | 白文字 + 黒縁（stroke 3px） |
| 生成方法 | AI字幕（CapCut自動キャプション） |
| 強調演出 | キーワードのみ色変更（赤 or 黄） |
| アニメ | 落下IN（Hook）/ フェードイン（本題）/ テキストカード（CTA） |

### BGM

| 項目 | 設定値 |
|------|--------|
| ジャンル | Lo-fi ピアノ / チル系 |
| 音量 | -18dB（ボイス優先）|
| タイミング | カットのビートに合わせて配置 |
| フェード | CTAで3秒かけてフェードアウト |

### 推奨編集ソフト

- **CapCut**：AI字幕・テンプレート豊富、無料
- **Adobe Premiere Pro**：細かい調整向け
- **DaVinci Resolve**：カラーグレーディング重視の場合

---

## 3. AI×アフィリエイト専用プロンプト集

### ナレーション生成

```
あなたはTikTokのプロ動画クリエイターです。
以下の条件でナレーション原稿を書いてください。

- テーマ：[テーマを入力]
- 尺：[秒数]秒
- ターゲット：副業・フリーランス志望の20〜35歳
- トーン：親しみやすい、テンポ良い、押しつけがましくない
- 構成：Hook(3秒) / 本題 / CTA(5秒)
- 禁止：専門用語、長い一文、フォーマルな敬語
```

### テロップ文生成

```
以下のナレーション原稿から、TikTokテロップ用の短文を抜き出してください。
- 1テロップ = 最大15文字以内
- 体言止め or 短い文末で終わる
- 強調したい単語には【】をつける

原稿：[原稿を貼る]
```

### Hook強化

```
以下のHookをより視聴継続率が高くなるよう3パターンに書き直してください。
- 冒頭3秒で「自分ごと」に感じさせること
- ネガティブな感情（焦り・損失感）を軽く刺激すること
- 25文字以内

元Hook：[Hookを入力]
```

### 動画タイトル＆ハッシュタグ生成

```
以下の動画内容に合ったTikTokタイトルとハッシュタグを生成してください。
- タイトル：30文字以内、検索されやすいキーワードを含む
- ハッシュタグ：5〜8個、大きめタグ×2 + ニッチタグ×3〜5の構成

動画内容：[内容を入力]
```

---

## 4. GitHub連携ワークフロー

### リポジトリ構成

```
claude-code-video-toolkit/
├── scripts/        # ナレーション・テロップ原稿
├── slides/         # スライド自動生成ツール（build.js + specs/）
├── assets/         # BGM・フォント・素材リスト
├── storyboards/    # ストーリーボード（Markdown）
├── prompts/        # 再利用プロンプト集
└── exports/        # 完成動画メモ・投稿記録
```

### スライド生成

全画面テキストのスライドは `slides/` のツールで生成する。詳細は `slides/README.md`。

```bash
node slides/build.js day01           # 1080x1920のPNGを出力
node slides/build.js day01 --guide   # TikTok UIとの干渉チェック
```

**Claudeはスライドを作ったら必ず `Read` でPNGを開いて目視確認すること。**
文字のはみ出し・色の潰れ・セーフゾーン侵食はログに出ないことがある。

### 動画の書き出し

CapCutを使わず ffmpeg で完成させる。詳細は `video/README.md`。

```bash
node video/build.js day01                                  # 無音プレビュー
node video/build.js day01 --voice rec.m4a --bgm lofi.mp3 --fit
```

**Claudeは書き出したら必ず ffprobe で尺・解像度を確認し、
フレームを抜いて `Read` で目視確認すること。**

### 作業フロー

1. `scripts/` にナレーション原稿を保存（`YYYY-MM-DD_テーマ.md`）
2. 編集完了後 `storyboards/` にストーリーボードをコミット
3. 投稿後 `exports/` に実績メモ（再生数・フォロワー増減）を記録
4. 週次でプロンプトを `prompts/` に整理・追加

### よく使うコマンド

```bash
# 新しい動画プロジェクトを作成
git checkout -b video/YYYY-MM-DD_テーマ名

# 素材をステージング
git add scripts/ storyboards/

# コミット
git commit -m "add: [テーマ名] 動画原稿・ストーリーボード"

# プッシュ
git push origin HEAD
```

### GitHub連携状態

- アカウント: ryo-chun
- 認証: gh CLI（keyring）
- スコープ: repo / gist / workflow

---

## 動作ルール

- ストーリーボードを作るときは必ずビジュアルウィジェットで表示する
- 原稿生成はテーマを聞いてから着手する
- スペックは上記テーブルをデフォルトとし、変更があれば明示する
- ハッシュタグ・投稿時間の提案はユーザーが求めたときのみ行う
